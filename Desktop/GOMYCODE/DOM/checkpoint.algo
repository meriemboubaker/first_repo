ALGORITHM sentence_reader
VAR
    ch :string;
    s,i,j : integer:=0;
BEGIN
    REPEAT
    IF ((ch[i]='a')||(ch[i]='e')||(ch[i]='i')||(ch[i]='y')||(ch[i]='o')||(ch[i]='u'))THEN
    {s=s+1;}
    ELSE IF (ch[i]=' ')
    {j:=j+1;}
    i:=i+1;
    UNTIL (ch[i]='.')


END_WHILE
END

